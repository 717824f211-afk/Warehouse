Public class Warehouse {Private String whId;Private String location;Private int capacity;Private List<Item> inventory;Public Warehouse(String whId, String location, int capacity) {This.whId = whId;This.location = location;This.capacity = capacity;This.inventory = new ArrayList<>();}Public void addItem(Item item) {Inventory.add(item);}Public Item findItem(String sku) {For (Item I : inventory) {If (i.getSku().equalsIgnoreCase(sku)) return I;}Return null;}Public void receive(Item item, int qty) {Item.receive(qty);If (!inventory.contains(item)) {Inventory.add(item);}}Public boolean pickPack(String sku, int qty) {Item I = findItem(sku);If (I != null) {Return i.reduceStock(qty);}Return false;}Public void showInventory() {System.out.println(“Warehouse “ + whId + “ Inventory:”);For (Item I : inventory) {System.out.println(“ – “ + i);}}}
